## Lesson 1 - Primitive types

We can learn about the types inside the microsoft website itself in the tutorials.

The number types go from byte to long, the unsigned types, the ones that starts with u are only used for positive numbers.